<Window x:Class="EldenRingLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Elden Ring Launcher" Height="500" Width="900"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Colors based exactly on the Python script -->
        <SolidColorBrush x:Key="GoldBrush" Color="#C8A23C"/>
        <SolidColorBrush x:Key="GoldDimBrush" Color="#6E5A1E"/>
        <SolidColorBrush x:Key="DarkBgBrush" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="PanelBgBrush" Color="#0F0F0F"/>
        <SolidColorBrush x:Key="CardBgBrush" Color="#181818"/>
        <SolidColorBrush x:Key="CardHoverBrush" Color="#222222"/>
        <SolidColorBrush x:Key="CardBorderBrush" Color="#2A2218"/>
        <SolidColorBrush x:Key="TextMainBrush" Color="#E8DFC8"/>
        <SolidColorBrush x:Key="TextSubBrush" Color="#8A7F6A"/>

        <!-- Card Button Style -->
        <Style TargetType="Button" x:Key="CardButton">
            <Setter Property="Background" Value="{StaticResource CardBgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="58"/>
            <Setter Property="Margin" Value="4,4,4,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource CardHoverBrush}"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerButton">
            <Setter Property="Background" Value="#4A1515"/>
            <Setter Property="Foreground" Value="#C09090"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6B2222"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" x:Key="SuccessButton" BasedOn="{StaticResource CardButton}">
            <!-- Just reuse card button for custom mod but with green accent below -->
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource DarkBgBrush}">
        <!-- Background Image -->
        <Image Source="pack://application:,,,/bg.png" Stretch="UniformToFill"/>
        
        <!-- Right Gradient Overlay to ensure text readability if panel is transparent, but here panel is solid -->
        <Border HorizontalAlignment="Right" Width="400">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#000A0A0A" Offset="0.0"/>
                    <GradientStop Color="#FF0A0A0A" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- Main UI (Right Panel) -->
        <Border x:Name="MainUI" HorizontalAlignment="Right" Width="320" Background="{StaticResource PanelBgBrush}" BorderBrush="{StaticResource GoldDimBrush}" BorderThickness="2,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Content="✕" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,5,5,0"
                        Background="Transparent" Foreground="{StaticResource TextSubBrush}" FontSize="16" FontWeight="Bold" BorderThickness="0"
                        Click="CloseButton_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Header -->
                <Grid Grid.Row="1" Margin="18,30,18,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="LAUNCH" Foreground="{StaticResource GoldBrush}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Row="1" Text="Choose your path" Foreground="{StaticResource TextSubBrush}" FontSize="11" Margin="2,2,0,0"/>
                    <Border Grid.Row="2" Height="1" Background="{StaticResource GoldDimBrush}" Margin="0,10,0,14"/>
                </Grid>

                <!-- Buttons Stack -->
                <ScrollViewer Grid.Row="2" Margin="8,0,8,8" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="BtnStack">
                        <!-- Vanilla -->
                        <Button Style="{StaticResource CardButton}" Click="BtnVanilla_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource GoldDimBrush}" Width="3" Height="40" CornerRadius="2" Margin="10,0,8,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Elden Ring" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}"/>
                                    <TextBlock Text="Launch via EAC (Online)" FontSize="10" Foreground="{StaticResource TextSubBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Button>

                        <!-- Offline Mode -->
                        <Button Style="{StaticResource CardButton}" Click="BtnOffline_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource GoldDimBrush}" Width="3" Height="40" CornerRadius="2" Margin="10,0,8,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Offline Mode" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}"/>
                                    <TextBlock x:Name="TxtOfflineStatus" Text="Click to locate" FontSize="10" Foreground="{StaticResource TextSubBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Button>

                        <!-- Seamless Co-op -->
                        <Button Style="{StaticResource CardButton}" Click="BtnCoop_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource GoldDimBrush}" Width="3" Height="40" CornerRadius="2" Margin="10,0,8,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Seamless Co-op" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}"/>
                                    <TextBlock x:Name="TxtCoopStatus" Text="Click to locate" FontSize="10" Foreground="{StaticResource TextSubBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Button>

                        <!-- Convergence -->
                        <Button Style="{StaticResource CardButton}" Click="BtnConvergence_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource GoldDimBrush}" Width="3" Height="40" CornerRadius="2" Margin="10,0,8,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="The Convergence" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}"/>
                                    <TextBlock x:Name="TxtConvergenceStatus" Text="Click to locate" FontSize="10" Foreground="{StaticResource TextSubBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Button>

                        <Border Height="1" Background="#1E1E1E" Margin="10,16,10,8"/>

                        <!-- Add Custom Mod -->
                        <Button Style="{StaticResource CardButton}" Click="BtnAddMod_Click">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="#2ea043" Width="3" Height="40" CornerRadius="2" Margin="10,0,8,0"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="+ Add Custom Mod" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextMainBrush}"/>
                                    <TextBlock Text="Select a .exe or .bat file" FontSize="10" Foreground="{StaticResource TextSubBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <!-- Footer -->
                <Grid Grid.Row="3" Margin="16,0,16,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Uninstall" Style="{StaticResource DangerButton}" Width="100" Height="28" FontSize="11" Click="BtnUninstall_Click"/>
                    <TextBlock Grid.Column="1" Text="♥ Endorse this Mod" Foreground="{StaticResource GoldDimBrush}" FontSize="11" HorizontalAlignment="Right" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="Endorse_Click"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Setup Overlay -->
        <Border x:Name="SetupOverlay" Background="#dd0A0A0A" Visibility="Collapsed">
            <Border Background="{StaticResource PanelBgBrush}" BorderBrush="{StaticResource GoldDimBrush}" BorderThickness="1" CornerRadius="14" Width="420" Height="290" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect><DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="0"/></Border.Effect>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
                    <TextBlock Text="Welcome, Tarnished" Foreground="{StaticResource GoldBrush}" FontSize="24" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,2"/>
                    <TextBlock Text="FIRST TIME SETUP" Foreground="{StaticResource GoldDimBrush}" FontSize="11" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,8"/>
                    <Border Height="1" Background="#332A15" Margin="40,0,40,16"/>
                    <TextBlock Text="Click below to locate your game's" Foreground="{StaticResource TextSubBrush}" FontSize="12" TextAlignment="Center" Margin="0,0,0,2"/>
                    <TextBlock Text="start_protected_game.exe" Foreground="{StaticResource GoldDimBrush}" FontSize="13" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,2"/>
                    <TextBlock Text="The original will be safely renamed." Foreground="{StaticResource TextSubBrush}" FontSize="12" TextAlignment="Center" Margin="0,0,0,20"/>
                    <Button Content="Browse &amp; Install" Background="{StaticResource GoldBrush}" Foreground="{StaticResource DarkBgBrush}" FontSize="15" FontWeight="Bold" Height="44" Width="300" BorderThickness="0" Cursor="Hand" Click="BtnSetupBrowse_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="8"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Border>

        <!-- Custom Dialog Overlay -->
        <Border x:Name="DialogOverlay" Background="#dd0A0A0A" Visibility="Collapsed">
            <Border Background="{StaticResource PanelBgBrush}" BorderBrush="{StaticResource GoldDimBrush}" BorderThickness="1" CornerRadius="12" Width="400" MinHeight="200" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                <Border.Effect><DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="0"/></Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="DialogTitle" Grid.Row="0" Text="Title" Foreground="{StaticResource GoldBrush}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock x:Name="DialogMessage" Grid.Row="1" Text="Message" Foreground="{StaticResource TextSubBrush}" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,25"/>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="DialogBtnNo" Content="Cancel" Foreground="{StaticResource TextMainBrush}" Background="#2A2A2A" Margin="0,0,10,0" Width="100" Height="35" Click="DialogBtnNo_Click" Visibility="Collapsed">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="6"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Button.Template>
                        </Button>
                        <Button x:Name="DialogBtnYes" Content="OK" Foreground="{StaticResource DarkBgBrush}" Background="{StaticResource GoldBrush}" Width="100" Height="35" FontWeight="Bold" Click="DialogBtnYes_Click">
                            <Button.Template><ControlTemplate TargetType="Button"><Border Background="{TemplateBinding Background}" CornerRadius="6"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border></ControlTemplate></Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>
