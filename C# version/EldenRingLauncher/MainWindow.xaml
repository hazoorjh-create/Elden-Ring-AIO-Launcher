<Window x:Class="EldenRingLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Elden Ring Custom Launcher" Height="480" Width="750"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <!-- Modern Button Style (CustomTkinter inspired) -->
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#1f538d"/>
            <Setter Property="Foreground" Value="#DCE4EE"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#14375e"/>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SuccessButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2ea043"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#237a33"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerButton" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#da3633"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#b82e2b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12" Background="#242424" ClipToBounds="True">
        <Grid>
            <!-- Background Image -->
            <Image Source="pack://application:,,,/bg.png" Stretch="UniformToFill" Opacity="0.15"/>
            
            <!-- Main UI -->
            <Grid x:Name="MainUI">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Custom Title Bar -->
                <Grid Grid.Row="0" Background="#33000000">
                    <TextBlock Text="Elden Ring AIO Launcher" Foreground="#DCE4EE" FontSize="15" FontWeight="SemiBold" Margin="20,12" VerticalAlignment="Center"/>
                    <Button Content="✕" Width="40" Height="40" HorizontalAlignment="Right" VerticalAlignment="Top"
                            Background="Transparent" Foreground="#DCE4EE" FontSize="16" FontWeight="Bold" BorderThickness="0"
                            Click="CloseButton_Click" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Main Buttons -->
                <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="340">
                    <Button x:Name="BtnVanilla" Content="Play Vanilla Game" Style="{StaticResource ModernButton}" Height="45" Margin="0,0,0,15" Click="BtnVanilla_Click"/>
                    <Button x:Name="BtnOffline" Content="Play Offline Mode" Style="{StaticResource ModernButton}" Height="45" Margin="0,0,0,15" Click="BtnOffline_Click"/>
                    <Button x:Name="BtnCoop" Content="Play Seamless Co-op" Style="{StaticResource ModernButton}" Height="45" Margin="0,0,0,15" Click="BtnCoop_Click"/>
                    <Button x:Name="BtnConvergence" Content="Play The Convergence" Style="{StaticResource ModernButton}" Height="45" Margin="0,0,0,15" Click="BtnConvergence_Click"/>
                    <Button x:Name="BtnAddMod" Content="Add / Play Custom Mod" Style="{StaticResource SuccessButton}" Height="45" Click="BtnAddMod_Click"/>
                </StackPanel>

                <!-- Footer / Settings -->
                <Border Grid.Row="2" Background="#44000000" Padding="20">
                    <Grid>
                        <CheckBox x:Name="ChkAutoClose" Content="Auto-Close Launcher on start" Foreground="#DCE4EE" FontSize="13" VerticalAlignment="Center" Checked="ChkAutoClose_Changed" Unchecked="ChkAutoClose_Changed"/>
                        <Button Content="Uninstall Launcher" Style="{StaticResource DangerButton}" HorizontalAlignment="Right" Padding="15,6" FontSize="13" Click="BtnUninstall_Click"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Setup Overlay -->
            <Border x:Name="SetupOverlay" Background="#dd1a1a1a" Visibility="Collapsed">
                <Border Background="#2b2b2b" CornerRadius="12" Width="450" Height="250" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="0"/>
                    </Border.Effect>
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="30">
                        <TextBlock Text="First Time Setup" Foreground="#DCE4EE" FontSize="22" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="To install the launcher, we need to locate your Vanilla game executable. Please browse and select your original start_protected_game.exe file." 
                                   Foreground="#A6A6A6" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,30"/>
                        <Button Content="Browse for start_protected_game.exe" Style="{StaticResource SuccessButton}" Height="45" Width="300" Click="BtnSetupBrowse_Click"/>
                    </StackPanel>
                </Border>
            </Border>

            <!-- Custom Dialog Overlay (Replaces MessageBox) -->
            <Border x:Name="DialogOverlay" Background="#dd1a1a1a" Visibility="Collapsed">
                <Border Background="#2b2b2b" CornerRadius="12" Width="400" MinHeight="200" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.5" ShadowDepth="0"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock x:Name="DialogTitle" Grid.Row="0" Text="Title" Foreground="#DCE4EE" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                        <TextBlock x:Name="DialogMessage" Grid.Row="1" Text="Message" Foreground="#A6A6A6" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,25"/>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="DialogBtnNo" Content="Cancel" Style="{StaticResource ModernButton}" Background="#4a4a4a" Margin="0,0,10,0" Width="100" Click="DialogBtnNo_Click" Visibility="Collapsed"/>
                            <Button x:Name="DialogBtnYes" Content="OK" Style="{StaticResource SuccessButton}" Width="100" Click="DialogBtnYes_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>
